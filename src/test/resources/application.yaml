spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/local_incident_test
    username: postgres
    password:
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
